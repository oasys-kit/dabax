========
Examples
========

Examples are available in the ``examples/`` directory of the repository:

* ``dabax_examples.py`` — Basic usage of the dabax API
* ``dabax_examples_xraylib_api.py`` — Using dabax with the xraylib-compatible API
* ``refraction_index.py`` — Computing refraction indices
